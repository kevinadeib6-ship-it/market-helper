services:
  - type: web
    name: market-helper-phone
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run phone_market_helper.py --server.address 0.0.0.0 --server.port $PORT
